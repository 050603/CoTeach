# openPBL 设计系统

openPBL 采用“现代教育出版物 + 安静的数字课堂”语言。界面通过教学关系、阶段状态和过程证据呈现 AI，不使用机器人、发光、蓝紫渐变或装饰性 Sparkles 制造 AI 感。

## 内部教学界面基础 token

| 语义 | Token | 色值 |
| --- | --- | --- |
| 页面背景 | `--pbl-bg` | `#F4F2ED` |
| 内容表面 | `--pbl-surface` | `#FCFBF8` |
| 正文 | `--pbl-text` | `#1F2933` |
| 次级文本 | `--pbl-text-muted` | `#5F6B76` |
| 边框 | `--pbl-border` | `#D8D6D0` |
| 教师 | `--pbl-teacher` | `#344A6A` |
| 学生 | `--pbl-student` | `#2F6F65` |
| AI | `--pbl-ai` | `#566F7F` |
| 警告 | `--pbl-warning` | `#8A6422` |
| 危险 | `--pbl-danger` | `#8A3E3E` |

正文使用 Noto Sans SC，课程标题和数字教案章节标题使用 Noto Serif SC。字重只使用 400、500、600、700。圆角只使用 6、10、14px；普通内容不使用阴影，阴影保留给固定操作栏、Drawer、Dialog 和 Toast。

## 内部教学组件原则

- 一个页面只保留一个高强调主操作。
- 普通内容用留白、段落和细边框建立层次，不嵌套无意义卡片。
- 教师、学生、AI 的角色色只表示责任与状态，不作大面积背景。
- 所有异步操作显示保存中、已保存、失败或可重试状态。
- 危险操作使用 AlertDialog；字段编辑使用 Dialog 或 Drawer；短暂结果使用 Toast。
- 所有图标按钮提供可访问名称，触控目标不小于 44px，焦点轮廓不可被移除。

## 内部教学布局

全局无传统侧边栏。顶部为课堂上下文栏：课程身份、当前阶段/任务、保存和课堂工具。长流程底部使用统一操作栏。系统仅支持桌面访问；较窄桌面窗口可折叠辅助区，1280px 以上允许项目工作空间三栏，无需手机适配。

## 新增课程平台：现代学习空间

新增教师、学生外层采用独立的平台主题。下列规则只适用于课程组织、课程库、成员、活动、学习记录、成果评价、账号及设置页面；备课、授课和沉浸式学习仍保留原来的布局、色彩和动效。上文关于无侧边栏、圆角和普通内容阴影的约束属于内部教学界面，不限制外层平台。

### 色彩与布局

- 深墨蓝 `#172A43` 导航、浅灰蓝 `#F3F6FA` 画布、白色内容表面；正文 `#29394F`，次级文字 `#617188`。
- 教师蓝 `#365FCA`、学生青绿 `#16766D` 为角色主色，暖金只用于课程主题插画。以品牌区域、课程封面和内容层级体现教学身份，不依赖空白与按钮堆叠。
- 仅支持桌面访问，采用单一左侧常驻导航，不设置顶部常驻栏。Logo 与返回上级入口固定在侧栏顶部，课程、课程库和设置入口位于其下；页面内容最大宽 1480px，按 1024、1280、1440px 桌面窗口验收。表格在自身容器内横向滚动。本次不增加手机适配或移动端访问拦截。
- 标题、正文统一使用现有中文无衬线字体。控件圆角 10px，内容表面 16–22px；轻阴影区分悬浮或可交互表面，避免多层卡片嵌套。
- 列表以课程封面和内容身份为主，详情以课程身份、章节、活动为主；统计只展示接口已有的真实数据，不制造教学指标。

### 组件与动效

外层组件位于 `src/components/platform`。`platform.css` 的样式必须限定在 `.pbl-platform-theme` 下；Portal 弹窗显式添加主题类。设置页使用平台主题但保留身份 SessionProvider；原备课与授课路由不包平台主题。

- 共享导航、标题区、封面插画、内容表面、状态反馈与加载骨架。课程没有封面时使用代码绘制的教学图形；教师可在课程详情根据名称、简介、大纲和学期生成或重新生成课程封面。课堂封面只读取备课发布快照中的封面，保证教师入口、学生入口和历史场次展示同一张图。
- 一个活动只有一个高强调主入口；组织编辑收进“更多”。任务、状态和结果有明确的标题与说明。
- 页面进入 320ms、悬停与展开 180–200ms；不使用持续装饰动画。`prefers-reduced-motion` 关闭动画、位移和过渡。
- 操作目标至少 44px，键盘焦点清晰；空数据、加载、失败与成功均提供相应反馈，错误时可重试，不把加载中的计数显示为实际零。

### 课堂职责

外层负责组织课程、选择课堂和查看学习记录。开始、课堂配置、结束、再次授课集中在 `/teacher/teach/[id]/setup` 和原授课工作台内。课堂活动根据状态显示“进入课堂”“继续授课”或“查看课堂记录”；首次进入创建待授课场次，不自动开课。

模板版本是教学设计快照，运行配置属于当前场次；再次授课新建场次。学生在原 PBL 课堂学习，已结束场次进入只读成果回顾。其他教案格式使用兼容工作台，不强制五阶段结构。
