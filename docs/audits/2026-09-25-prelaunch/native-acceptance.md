# Windows / macOS 实机补验单

本单尚未执行，所有项目状态为“阻塞”。Linux 上的 Chromium、Firefox、WebKit 引擎测试和 DPR 模拟不能代替操作系统、字体、输入法、浏览器原生缩放或触控板实测。实机只使用专用测试账号与隔离课程；不要在正式业务课程中创建或删除数据。

| 项目 | 实机操作与通过条件 | 状态 |
|---|---|---|
| Windows 浏览器 | 记录 Windows 版本及 Chrome、Edge、Firefox 实际版本；教师、学生分别完成登录、课程、活动、课堂、预览的直达和刷新。无白屏、资源缺失或无法操作。 | 阻塞 |
| macOS 浏览器 | 记录 macOS 版本及 Safari、Chrome、Firefox 实际版本；重复上述双角色入口和关键操作。 | 阻塞 |
| 浏览器原生缩放 | 100%、125%、150%、200% 分别检查 768 像素分屏、1024×576、常规笔记本和 4K 窗口；正文、弹窗、底部操作、投屏栏、PDF 和播放器不得裁切，长表格可局部滚动。 | 阻塞 |
| 系统显示缩放 | Windows/macOS 真实系统缩放分别检查登录、课程详情、课堂五阶段、编辑器和设置弹窗；记录有效 CSS 视口与屏幕 DPR。 | 阻塞 |
| 中文输入法组合输入 | 在课程标题、作业/问卷答案、文档成果和 Monaco 代码编辑器中使用拼音组合、候选选择、撤销、保存、刷新；不得提前提交、丢字或重复输入。 | 阻塞 |
| 键盘与触控板 | Tab/Shift+Tab/Enter/Escape 操作菜单、弹窗和课堂；触控板纵向/横向滚动、惯性滚动和分屏调整后，焦点与主要按钮始终可达。 | 阻塞 |
| 媒体与权限 | 实际播放 WAV/WebM/PDF，拒绝后再允许麦克风和全屏权限；失败提示明确，返回页面后可重试。本版没有摄像头采集入口，新增后另行验收。 | 阻塞 |

每个执行记录须包含日期、系统与浏览器版本、屏幕分辨率、系统/浏览器缩放、角色、页面地址、操作结果、截图或短视频及“通过／失败／阻塞／不适用”。出现失败时保留控制台错误和对应请求，再复验修复后的同一组合。
