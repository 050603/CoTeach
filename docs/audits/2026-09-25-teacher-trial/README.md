# “测试课程”老师试用前验收记录（2026-09-25）

本轮只评估已有课程的授课与听课。目标课程为“测试课程”，课堂活动为“中小学人工智能教育的教学理论与方法”。实际分享地址为 `https://coteach.cn`。当前开放的是新活动 `7255066e-4d2c-40fa-9db7-7641fb9ae74b`、新场次 `722e9310-e696-453d-bbd4-712eb5deb3f0`，绑定课堂模板版本 2；此前版本 1 的活动 `575ab1e0-ce8a-432b-8a29-20d61ba23f76` 已按用户意图结束并归档，历史记录保留。本轮不做压测、容量、长稳或课程生成验收。用户已明确 AI 课堂右侧对话是已移除版本的功能，本次不测试、不恢复该入口，其临时探查结果不计入验收。测试基线起于 `cc4c726`，包含本目录记录的工作区修复。当前共享服务与隔离核心联测验证构建 `DmvLKJ4RJmcuQ0Ac06Xys`；整课 1 倍速只读预览固定于较早构建 `gk2iVvpiSU00WtRwu_xxq`，两种证据不可混同。发送前须再次固定当时运行版本。任何旧报告只作为背景，不计入当前版本的实测通过数。

**当前结论：继续准备，尚不能发送老师试用。** 用户先确认旧场次有意结束、归档，随后明确恢复试用准备。19:29 的只读预检再次确认新活动开放、新场次授课中、版本 2 的 26 个场景及资源完整；学生真实登录已从课程列表进入新活动，教师切回讲授阶段后，现场音频播放、暂停和恢复也通过。当前共享构建上，学生直达活动链接首次复验有 1 次登录后回普通登录页，随后 4 次通过，仍列为偶发待查。旧活动不再是试用目标，也不作为当前系统故障。新场次学生实际完整听课与提交、教师结果核对及实机验收尚未全部完成。下表把新场次、固定构建预览、隔离业务与模拟页面的证据分开。原始机器报告和截图写入 Git 忽略的 `test-results/teacher-trial/`；这些文件仅在当前测试主机保留，删除后须复跑，不把报告中的统计当作永久附件。
按原计划 A1–C10 及互动、故障、设备逐项追踪见[覆盖清单](coverage.md)。
发现与待办的复现、严重程度及复验状态见[问题清单](issues.md)。

本轮的功能清单从这门课已发布的课堂快照和当前页面入口确定，不按旧设计文案推断功能。快照配置了“项目启动”（1 份 PPTX/PDF 预览资源）、“知识讲授”（18 页配音课件、8 组测验）、“项目实践”（文档模式）、“成果汇报与评价”和“学习反思”五个阶段。后面三个阶段的实际按钮、保存与教师查看仍待本课教师会话验证，配置存在本身不算功能通过。当前学生画面右侧的 AI Teacher 是讲解、字幕与播放控制区；已移除的右侧**互动对话**不在清单中，不发起问答、断流测试，也不为它加回代码。代码实验不是本课的文档模式内容。版本 2 的教师阶段任务文案仍有“向 AI 讲师提问”等旧文字，须在教师页面核对是否展示并让老师误以为可以提问。

| 范围 | 已取得的证据 | 尚需完成 |
|---|---|---|
| 课程与资源 | 新活动的只读预检全部通过：版本 2 仍有 26 个学生场景、18 页、8 组测验、16 道题、85 段可解析配音和 3 张可解码插图；题目、答案与解析结构完整，未发现明显占位文本。19:29 再查活动仍开放、场次仍授课中，见 `course-preflight-final-20260925/report.json`。共用的启动 PDF 9 页曾逐页查看无空白、明显裁切或模糊，见 `launch-pdf-preview/`。 | 知识讲授视觉与内容、真实投屏仍需人工核对。启动 PDF 第 5 页页脚误写“5/10”，属非阻断瑕疵。 |
| 学生真实入口 | 新场次在实际域名由专用学生账号登录，从课程列表进入活动和课堂；启动 PDF、3 张插图及音频文件均可读取，匿名请求被拒绝。教师切回讲授阶段后，实际音频播放、暂停、切换标签页后继续通过；打开讲授页面没有误记完成，刷新与新浏览器登录仍看到空进度。旧共享构建的直达链接通过；当前构建首次 1 次回登录页、随后 4 次落回活动，归档旧活动有错误和返回课程入口。见 `live-student-v2-lecture-rechecked/report.json`、`live-student-v2-deep-link-dmv/report.json`、`live-student-v2-deep-link-dmv-repeat-{1,2,3}/report.json`。 | 因新场次尚无已完成章节，完成进度的持久化恢复**未验证**；还需实际完成章节或测验后重进。直达链接偶发问题、完整听课、测验保存、最小化窗口及断网恢复仍待验证。 |
| 整课只读预览 | 当前模板版本 2 的 26 个场景、85 段实际 WAV 和 3 张插图，以只读夹具在固定构建 `gk2iVvpiSU00WtRwu_xxq` 的 Chromium 1 倍速完成：85 段顺序播完、8 组测验完成，0 重播、0 页面错误、0 资源失败、0 写入，31.5 分钟音频、总测试 33.7 分钟；见 `full-playback-v2-gk2-1x/report.json`。 | 此证据使用页面预览与模拟业务接口；当前共享构建为 `DmvLKJ4RJmcuQ0Ac06Xys`，现网学生完整播放、真实提交和记录恢复仍需复验。 |
| 图片访问 | 学生在真实课堂访问 3 张教材插图曾得到 404；已修复为仅允许已加入该课堂的学生读取教师拥有且实际嵌入学生课件的插图。定向单测 17 项通过；新构建发布后，指定学生从实际域名读取 3 张图均为 200，匿名请求被拒绝；源图逐张查看清晰、无缺损。见 `live-student-fresh-login/report.json`。 | 学生课件实际排版中的缩放与清晰度仍需逐页核对。 |
| 双角色与隔离 | 一次性 PostgreSQL、独立 Redis、上传目录及当前生产构建 `DmvLKJ4RJmcuQ0Ac06Xys` 中，以真实登录创建 2 位教师、2 位已加入学生和 1 位未加入学生；无效与停用邀请码、重复加入、重复开课和结束、教师切阶段后学生同步、教师刷新保持阶段、学生浏览器深层链接恢复、独立工作区、教师人数与越权边界共 14 项通过。只跑当前授课链路，见 `functional-final-dmv-deeplink/report.json`。 | 这门真实课程的教师 A 登录、授课控制、结果页面仍未由测试主机取得其会话；教师 B、学生 C 的现网验证也未做。构建再更新后需跑核心回归。 |
| 浏览器与网络 | 主机侧 HTTPS、登录页和静态资源可访问；Linux Chromium 已走实际学生入口。 | 独立外网电脑、Windows Chrome/Edge、macOS Safari、系统与浏览器缩放、中文输入法和不参与开发者的操作观察。由我方后续安排实机验收。 |
| 页面适配 | Linux Chromium、Firefox 在 768×576、1024×576、1440×900 的学生播放器、教师课堂和预览页面共 18 项模拟接口检查通过，见 `layout-{chromium,firefox}/summary.json`。脚本对已取消的页面目录按钮的旧断言已调整。 | Linux 主机缺少 WebKit 依赖，未完成该引擎复验；Windows/macOS 原生浏览器及缩放仍需实机，模拟页面不证明真实课程数据操作。 |

图片问题和学生登录深层链接的原缺陷已修复；后者在当前构建出现 1 次偶发回登录页，仍需查明。撤回误入范围的右侧对话改动后，`pnpm build` 及 CSS 完整性检查通过；共享服务已切换到构建 `DmvLKJ4RJmcuQ0Ac06Xys`。本机 `/api/health/live`、含数据库、Redis、文件及模型依赖的 `/api/health/ready` 与实际域名登录页在本轮复验时均返回 200，健康接口仍不能替代真实播放与保存验收。登录跳转及插图权限的定向单测 19 项通过，相关 Lint、脚本语法检查及差异检查通过；涉及类型的前序检查 `pnpm typecheck` 通过。本轮测试脚本只使用用户指定的学生体验账号做真实入口检查，未在现网结束场次或提交测验；用户已确认旧场次的结束与归档是预期操作。学生账号在旧场次已有进度，新场次当前没有已完成讲授章节；不能擅自清除已有学习记录。教师账号被描述为“现在这个账号”，但自动化测试主机没有该账号的凭据或可复用浏览器会话；隔离环境里的教师登录不能替代它。

复验命令（密码仅通过环境变量临时传入，不写在仓库或报告）：

```bash
TRIAL_OFFERING_ID=<教学班ID> TRIAL_ACTIVITY_ID=<活动ID> TRIAL_OUTPUT_DIR=<新目录> node scripts/audit-teacher-trial-course.mjs
TRIAL_FUNCTIONAL_OUTPUT_DIR=<新目录> node scripts/verify-teacher-trial-functional.mjs
TRIAL_STUDENT_USERNAME=<学生学号> TRIAL_STUDENT_PASSWORD=<临时环境变量> TRIAL_OFFERING_ID=<教学班ID> TRIAL_ACTIVITY_ID=<活动ID> TRIAL_ENTER_CLASSROOM=1 TRIAL_MEDIA_PATH=<实际音频路径> TRIAL_IMAGE_IDS=<逗号分隔的插图ID> TRIAL_LAUNCH_RESOURCE_ID=<项目启动资源ID> TRIAL_OUTPUT_DIR=<新目录> node scripts/verify-teacher-trial-student.mjs
TRIAL_CLASSROOM_FILE=<已发布课堂JSON绝对路径> TRIAL_PLAYBACK_BASE_URL=<固定构建的本机地址> TRIAL_PLAYBACK_BUILD_ID=<构建号> TRIAL_PLAYBACK_OUTPUT_DIR=<新的报告目录> pnpm exec playwright test e2e/teacher-trial-full-playback.spec.ts --workers=1 --output=<新的Playwright目录>
```

最终放行复验应使用专用场次和干净学生记录，从教师实际登录开始，完成“开课—学生进入—播放—提交—教师查看—结束—重新登录查看记录”。全部必测项通过且实机记录齐备时才改为“可以试用”。[老师操作卡](teacher-guide.md)、[试用反馈模板](feedback-template.md)和[实机补验单](native-checklist.md)已备好，验收完成前不发送。
