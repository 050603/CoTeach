# 问题与待办

| 等级 | 现象与复现 | 状态与复验 |
|---|---|---|
| 阻断，已修复 | 已加入“测试课程”的学生打开第 11、15、23 页插图时，`/api/uploads/<id>` 返回 404；文件实际存在。上传读取权限只查模板资源引用，没有查已绑定课堂的学生课件图片。 | 代码修复并完成定向测试；更新生产构建后，用指定学生从实际域名读取 3 张图均返回 200，匿名访问被拒绝。仍需实机逐页看清晰度。 |
| 深层链接原缺陷已修复，偶发回登录页待查 | 匿名直达新课堂活动时，旧登录页忽略 `redirect`，登录后只回课程列表；代码已修复。共享服务切到 `DmvLKJ4RJmcuQ0Ac06Xys` 后首次复验有 1 次登录后回普通登录页，随后 4 次正常落回活动，隔离构建 1 次通过。 | 保留失败报告 `live-student-v2-deep-link-dmv/report.json`、后续通过报告 `live-student-v2-deep-link-dmv-diagnostic/report.json` 与 `live-student-v2-deep-link-dmv-repeat-{1,2,3}/report.json`。诊断脚本现记录跳转、响应状态和 cookie 名称；尚无足够证据认定根因，发送前须在固定构建与外网电脑复测。归档活动显示“活动不存在”及返回入口。 |
| 旧场次已归档，新场次已开放 | 18:27 旧场次从 `TEACHING` 变为 `FINISHED`，其活动随后关闭并归档；用户确认这是有意操作。新活动 `7255066e-4d2c-40fa-9db7-7641fb9ae74b` 已开放，新场次 `722e9310-e696-453d-bbd4-712eb5deb3f0` 已开启。 | 旧状态见 `course-preflight-after-clarification/report.json`；18:51 新活动只读预检全部通过，见 `course-preflight-active-20260925/report.json`。指定学生已从真实域名进入新场次。旧场次保持归档，不作为当前缺陷。 |
| 已归档场次的失效链接 | 学生直达已结束课堂仍可看到“返回课堂活动完成后测”，点击目标是已归档活动，页面实际显示“活动不存在”。 | 见 `finished-student-readonly/report.json`。若试用流程允许归档后的课堂回看，应修正此链接或给出有效返回入口；本轮不把它当成可用后测。 |
| 版本 2 整课只读预览已通过 | 旧模板版本 1 的只读整课 1 倍速播放曾通过；新场次绑定模板版本 2，课堂素材目录已变化。 | 固定构建 `gk2iVvpiSU00WtRwu_xxq` 的版本 2 预览 1 倍速完成 85 段音频、8 组测验，零重播、零页面和资源错误，见 `full-playback-v2-gk2-1x/report.json`。当前共享构建 `DmvLKJ4RJmcuQ0Ac06Xys` 的真实学生整课播放与提交仍待验证。 |
| 新场次现场播放已补验 | 指定学生首次从新活动进入课堂时，教师已切到“项目实践”，页面没有讲授按钮，测试脚本因此超时。 | 教师随后切回知识讲授；`live-student-v2-lecture-rechecked/report.json` 证实实际播放、暂停与恢复通过，旧超时不是播放器故障。新场次尚无已完成章节，完成记录的重进恢复仍未验证。 |
| 已排除本轮范围 | 曾误把 AI 课堂右侧对话当成本次试用功能进行探查。用户已确认这是已移除版本的入口，不属于当前系统交付功能。 | 已停止该项测试并撤回为其所做的代码修复；其观察结果不计入试用放行判断。 |
| 教师阶段任务文案待核对 | 新场次绑定的版本 2 快照仍在“知识讲授”阶段任务与要求中写“向 AI 讲师提问”；学生讲授场景没有这句，实际右侧仅见讲解、字幕和播放控制。教师阶段任务组件会读取课程要求，因此可能向教师展示已不存在的操作指引。 | 用现网教师会话确认展示位置；若可见，应通过课程内容新版本改为“听讲、完成随堂测验并记录关键概念”等实际步骤，再复验。不可为文案恢复已移除的互动对话。“项目实践”的 AI 组员按钮在当前页面实际存在，需按该入口判断相关描述。 |
| 轻微课件问题 | “项目启动”PDF 实际为 9 页，第 5 页页脚显示“5/10”，其余页脚按 9 页计。 | 主机渲染 9 页可正常阅读，不影响继续操作；建议修正该页码后重新生成预览，不因此恢复任何已移除功能。 |
| 测试脚本问题，已修正 | 首次 1 倍速整课预览播放完成 85 段音频和 8 组测验，但断言读取 `ended` 事件时的 `currentTime`；播放器在切段时可先将其重置为 0，导致脚本末尾误报失败。 | 改为记录每段音频播放中的最大时间后，版本 1 与版本 2 的固定构建均以 1 倍速完成 85 段、8 组测验。 |
| 测试脚本问题，已修正 | 深层链接首轮实测已成功回原活动，但脚本用不限定范围的 `getByRole('alert')` 读取归档页；Next.js 自带一个空的 route announcer，导致 Playwright strict mode 报错。 | 限定为包含“活动不存在”的提示后，实际域名复跑全部通过；首轮脚本错误不计为产品失败。 |
| 测试环境限制 | Linux 主机缺少 WebKit 所需音视频库，无法在本机启动该引擎。 | Chromium 与 Firefox 的 18 项桌面布局模拟通过；macOS Safari 真机验收仍由我方后续安排。 |
| 已恢复的短暂不可用 | 共享实例于 18:08 切换构建时，实际域名登录页一度返回 Nginx 502，导致一次学生浏览器检查未开始。 | `openpbl.service` 恢复后健康接口、登录页均返回 200；新构建上的学生登录、PDF、图片、音频及播放控制复验通过。正式试用期间应避免同时切换构建。 |
| 待验证 | 学生“测试课程”进入课堂时一次出现 `Failed to fetch`，随后重试成功；没有稳定复现步骤。 | 保留观察项；最终双角色冒烟及短断网复验时重点检查入口提示与重试。 |
| 放行条件未满足 | 现网教师账号仅被描述为“现在这个账号”，测试主机没有该会话或凭据；不能验收这门课的教师开课、课件控制、查看结果与结束。 | 用实际教师会话完成 B1–B8 和最终新场次冒烟；隔离教师账号通过不能替代。 |
| 放行条件未满足 | 提供的学生账号已产生本场次进度，当前并非空白首次听课。 | 发送老师前准备空白体验账号或明确接受现有记录；不擅自清空真实学习记录。 |
| 放行条件未满足 | 版本 2 的只读预览已完整以 1 倍速通过，但现网学生实际整课播放和测验保存、人工内容核对、独立外网电脑及 Windows/macOS 实机尚无通过记录。 | 完成覆盖清单与实机补验单，不能用只读预览或 Linux 引擎模拟替代现场验收。 |
